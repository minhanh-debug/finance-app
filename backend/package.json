{
  "name": "finance-app-backend",
  "version": "1.0.0",
  "description": "Finance App Backend - Node.js + Express + PostgreSQL",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma db push && node index.js",
    "dev": "nodemon index.js",
    "db:migrate": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^5.14.0"
  }
}
